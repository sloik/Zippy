name: Nightly build and test

on:
  schedule:
    - cron: '42 0 * * *'
  workflow_dispatch:

jobs:
  build:

    runs-on: macos-26

    steps:
    - uses: actions/checkout@v6
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
